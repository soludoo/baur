<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Odoo 19.0 no longer exposes the old 'sale.report' search view we inherited in v15.
             This part has been dropped to avoid referencing non-existing views/models. -->
        <!-- The 'account.invoice.report' model and its search view still exist in 19.0,
             but their internals have changed. To keep the module loading cleanly without
             guessing the new layout, this inheritance is removed as well. -->
    </data>
</odoo>